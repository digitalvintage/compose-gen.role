# Automatically generated by ansible 'compose-gen' role.
# Ansible managed! Do not modify - all changed will be overwritten by playbook!

[http]
  [http.middlewares]
    [http.middlewares.https-redirect.redirectscheme]
      scheme = "https"
    [http.middlewares.www-redirect.redirectregex]
      regex = "^https://www.(.*)"
      replacement = "https://$1"
    [http.middlewares.www-redirect-insecure.redirectregex]
      regex = "^http://www.(.*)"
      replacement = "http://$1"
    [http.middlewares.security-headers-a-plus.headers]
      # CORS
      AccessControlAllowMethods = ["GET", "OPTIONS", "PUT"]
      AccessControlAllowOrigin = "origin-list-or-null"
      AccessControlMaxAge = 100
      #AddVaryHeader = true
      BrowserXssFilter = true
      ContentTypeNosniff = true
      ForceSTSHeader = true
      FrameDeny = true
      SSLRedirect = true
      STSIncludeSubdomains = true
      STSPreload = true
      ContentSecurityPolicy = "default-src 'self'"
      CustomFrameOptionsValue = "SAMEORIGIN"
      ReferrerPolicy = "same-origin"
      FeaturePolicy = "vibrate 'self'"
      STSSeconds = 315360000
    [http.middlewares.security-headers-a.headers]
      # CORS
      AccessControlAllowMethods = ["GET", "OPTIONS", "PUT"]
      AccessControlAllowOrigin = "origin-list-or-null"
      AccessControlMaxAge = 100
      #AddVaryHeader = true
      BrowserXssFilter = true
      ContentTypeNosniff = true
      ForceSTSHeader = true
      FrameDeny = true
      SSLRedirect = true
      STSIncludeSubdomains = true
      STSPreload = true
      CustomFrameOptionsValue = "SAMEORIGIN"
      ReferrerPolicy = "same-origin"
      FeaturePolicy = "vibrate 'self'"
      STSSeconds = 315360000
